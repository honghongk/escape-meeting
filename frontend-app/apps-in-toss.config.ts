import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "escape-meeting",
  brand: {
    primaryColor: "#EF4D36",
  },
  webView: {},
  permissions: [],
  webBundleDir: "dist",
});
