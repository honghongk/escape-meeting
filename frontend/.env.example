# Leave empty for Docker Compose, which uses the same-origin Next.js proxy.
NEXT_PUBLIC_API_BASE_URL=

# Required for a static Apps-in-Toss bundle: use the public HTTPS backend URL.
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com